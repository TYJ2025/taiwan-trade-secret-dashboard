name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: npm install

      - name: Re-generate damages analysis
        run: |
          if [ -f trade_secret_judgments_fulltext.json ] && [ -f trade_secret_judgments_structured.json ]; then
            python3 extract_damages.py
          else
            echo "Skipping damages extraction — fulltext/structured JSON not present in repo."
          fi

      # 2026-06-11 新增：最高法院資料異動時自動重建見解 keyword 索引
      # （curated AI 摘要不在 CI 產製，新案件補產前頁面僅顯示全文與 snippet）
      - name: Rebuild holdings index
        run: |
          if [ -f config/holdings_topics.json ] && [ -f data/supreme_court_judgments_fulltext.json ]; then
            python3 scripts/build_holdings_index.py
          else
            echo "Skipping holdings index — config or SC fulltext not present."
          fi

      - name: Copy data to public
        run: mkdir -p public/data && cp -r data/* public/data/

      - name: Build
        run: npm run build

      - name: Copy data to dist
        run: cp -r data/* dist/data/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
